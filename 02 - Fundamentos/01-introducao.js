function soma(a, b) {
  //Função para somar dois números
  return a + b; //Retorna a soma dos dois números
}

const numero1 = 5; //Declaração de uma variavel chamada
const numero2 = 10;

const resultado = soma(numero1, numero2);
console.log(`A soma de ${numero1} e ${numero2} são ${resultado}.`);
